--!strict

-- Duration in seconds a pet capsule must incubate before the pet is awarded.
local PET_HATCH_DURATION: number = 15 * 60 -- 15 minutes

return {
	PET_HATCH_DURATION = PET_HATCH_DURATION,
}
